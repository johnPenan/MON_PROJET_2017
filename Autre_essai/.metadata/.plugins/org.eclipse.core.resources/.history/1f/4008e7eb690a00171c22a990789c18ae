/*
 * Creation : 16 mars 2017
 */
package com.jean.goumou.Mon_Projet_Maven;

public class Mon_Calcul {

    // Calcul de la division

    public double division(int a, int b) {
        return a / b;
    }

    // Calcul de la multiplication
    public int multiplication(int a, int b) {
        return a * b;
    }

    // Calcul de la soustraction
    public int substraction(int b, int c) {
        return b - c;

    }

    // Calcul de la racine carré
    public float squareRoot(float d) {
        return (float) Math.sqrt(d);
    }

}
